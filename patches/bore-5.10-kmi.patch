diff --git a/include/linux/sched.h b/include/linux/sched.h
index a91227eda..a1751ca53 100644
--- a/include/linux/sched.h
+++ b/include/linux/sched.h
@@ -494,10 +494,28 @@ struct sched_entity {
 	struct sched_avg		avg;
 #endif
 
+#ifdef CONFIG_SCHED_BORE
+	/*
+	 * BORE per-entity burst state, packed into GKI KABI-reserved slots. Under
+	 * __GENKSYMS__ ANDROID_KABI_USE collapses to the original u64 reserved
+	 * field, so every vendor-referenced CRC (and module_layout) is byte-
+	 * identical whether BORE is on or off — the vendor ABI is untouched.
+	 */
+	ANDROID_KABI_USE(1, u64 burst_time);
+	ANDROID_KABI_USE(2, struct {
+		u8 prev_burst_penalty;
+		u8 curr_burst_penalty;
+		u8 burst_penalty;
+		u8 burst_score;
+	});
+	ANDROID_KABI_RESERVE(3);
+	ANDROID_KABI_RESERVE(4);
+#else
 	ANDROID_KABI_RESERVE(1);
 	ANDROID_KABI_RESERVE(2);
 	ANDROID_KABI_RESERVE(3);
 	ANDROID_KABI_RESERVE(4);
+#endif /* CONFIG_SCHED_BORE */
 };
 
 struct sched_rt_entity {
diff --git a/init/Kconfig b/init/Kconfig
index 0d6aeefcb..e867e6eec 100644
--- a/init/Kconfig
+++ b/init/Kconfig
@@ -1234,6 +1234,20 @@ config CHECKPOINT_RESTORE
 
 	  If unsure, say N here.
 
+config SCHED_BORE
+	bool "Burst-Oriented Response Enhancer"
+	default n
+	depends on FAIR_GROUP_SCHED
+	help
+	  Modifies the CFS scheduler to discriminate tasks by their burst time
+	  (runtime since they last slept/yielded) and favor less-bursty, more
+	  interactive tasks (compositor, audio/video, input, games) under heavy
+	  background load. Burst state is stored in GKI KABI-reserved slots so the
+	  vendor ABI (module_layout) is preserved. Backported from
+	  firelzrd/bore-scheduler (bore5.1.0), minimal/atavistic-free.
+
+	  Experimental on this MediaTek EAS platform. If unsure, say N.
+
 config SCHED_AUTOGROUP
 	bool "Automatic process group scheduling"
 	select CGROUPS
diff --git a/kernel/sched/core.c b/kernel/sched/core.c
index 83cbce1e4..c4a49e1cb 100644
--- a/kernel/sched/core.c
+++ b/kernel/sched/core.c
@@ -3298,6 +3298,24 @@ int wake_up_state(struct task_struct *p, unsigned int state)
  *
  * __sched_fork() is basic setup used by init_idle() too:
  */
+#ifdef CONFIG_SCHED_BORE
+static void __init sched_init_bore(void) {
+	init_task.se.burst_time = 0;
+	init_task.se.prev_burst_penalty = 0;
+	init_task.se.curr_burst_penalty = 0;
+	init_task.se.burst_penalty = 0;
+	init_task.se.burst_score = 0;
+}
+
+static inline void sched_fork_bore(struct task_struct *p) {
+	p->se.burst_time = 0;
+	p->se.prev_burst_penalty = 0;
+	p->se.curr_burst_penalty = 0;
+	p->se.burst_penalty = 0;
+	p->se.burst_score = 0;
+}
+#endif // CONFIG_SCHED_BORE
+
 static void __sched_fork(unsigned long clone_flags, struct task_struct *p)
 {
 	p->on_rq			= 0;
@@ -3308,6 +3326,9 @@ static void __sched_fork(unsigned long clone_flags, struct task_struct *p)
 	p->se.prev_sum_exec_runtime	= 0;
 	p->se.nr_migrations		= 0;
 	p->se.vruntime			= 0;
+#ifdef CONFIG_SCHED_BORE
+	sched_fork_bore(p);
+#endif // CONFIG_SCHED_BORE
 	INIT_LIST_HEAD(&p->se.group_node);
 
 #ifdef CONFIG_FAIR_GROUP_SCHED
@@ -7474,6 +7495,11 @@ void __init sched_init(void)
 	BUG_ON(&dl_sched_class + 1 != &stop_sched_class);
 #endif
 
+#ifdef CONFIG_SCHED_BORE
+	sched_init_bore();
+	pr_info("BORE (Burst-Oriented Response Enhancer) CPU Scheduler modification 5.1.0 by Masahito Suzuki, backported to 5.10 (KMI-safe)\n");
+#endif // CONFIG_SCHED_BORE
+
 	wait_bit_init();
 
 #ifdef CONFIG_FAIR_GROUP_SCHED
diff --git a/kernel/sched/fair.c b/kernel/sched/fair.c
index d559f63e2..3cb3e5845 100644
--- a/kernel/sched/fair.c
+++ b/kernel/sched/fair.c
@@ -90,6 +90,98 @@ static unsigned int normalized_sysctl_sched_wakeup_granularity	= 1000000UL;
 
 const_debug unsigned int sysctl_sched_migration_cost	= 500000UL;
 
+#ifdef CONFIG_SCHED_BORE
+/*
+ * Burst-Oriented Response Enhancer (BORE) CPU Scheduler
+ * Backported from firelzrd/bore-scheduler (bore5.1.0) to android12-5.10.
+ * Minimal variant: no atavistic fork inheritance. Per-entity burst state lives
+ * in GKI KABI-reserved sched_entity slots (see include/linux/sched.h) so the
+ * vendor ABI (module_layout / CRCs) is byte-identical. Runtime on/off via the
+ * kernel.sched_bore sysctl (default on); when turned off, running entities
+ * decay back to their base weight.
+ */
+uint __read_mostly sched_bore                   = 1;
+uint __read_mostly sched_burst_smoothness_long  = 1;
+uint __read_mostly sched_burst_smoothness_short = 0;
+uint __read_mostly sched_burst_penalty_offset   = 22;
+uint __read_mostly sched_burst_penalty_scale    = 1280;
+
+#define MAX_BURST_PENALTY (39U << 2)
+
+static inline u32 log2plus1_u64_u32f8(u64 v) {
+	u32 msb = fls64(v);
+	s32 excess_bits = msb - 9;
+	u8 fractional = (0 <= excess_bits) ? v >> excess_bits : v << -excess_bits;
+	return msb << 8 | fractional;
+}
+
+static inline u32 calc_burst_penalty(u64 burst_time) {
+	u32 greed, tolerance, penalty, scaled_penalty;
+
+	greed = log2plus1_u64_u32f8(burst_time);
+	tolerance = sched_burst_penalty_offset << 8;
+	penalty = max(0, (s32)greed - (s32)tolerance);
+	scaled_penalty = penalty * sched_burst_penalty_scale >> 16;
+
+	return min(MAX_BURST_PENALTY, scaled_penalty);
+}
+
+static inline struct task_struct *task_of(struct sched_entity *se);
+
+static void update_burst_score(struct sched_entity *se) {
+	struct task_struct *p;
+	u8 prio, prev_prio, new_prio;
+
+	if (!entity_is_task(se))
+		return;
+	p = task_of(se);
+	prio = p->static_prio - MAX_RT_PRIO;
+	prev_prio = min_t(u8, 39, prio + se->burst_score);
+
+	se->burst_score = se->burst_penalty >> 2;
+
+	new_prio = min_t(u8, 39, prio + se->burst_score);
+	if (new_prio != prev_prio) {
+		/* this tree's reweight_task() takes a load_weight (see set_load_weight) */
+		struct load_weight lw = {
+			.weight = scale_load(sched_prio_to_weight[new_prio]),
+			.inv_weight = sched_prio_to_wmult[new_prio],
+		};
+		reweight_task(p, &lw);
+	}
+}
+
+static void update_burst_penalty(struct sched_entity *se) {
+	se->curr_burst_penalty = calc_burst_penalty(se->burst_time);
+	se->burst_penalty = max(se->prev_burst_penalty, se->curr_burst_penalty);
+	update_burst_score(se);
+}
+
+static inline u32 binary_smooth(u32 new, u32 old) {
+	int increment = new - old;
+	return (0 <= increment) ?
+		old + ( increment >> (int)sched_burst_smoothness_long) :
+		old - (-increment >> (int)sched_burst_smoothness_short);
+}
+
+static void restart_burst(struct sched_entity *se) {
+	se->burst_penalty = se->prev_burst_penalty =
+		binary_smooth(se->curr_burst_penalty, se->prev_burst_penalty);
+	se->curr_burst_penalty = 0;
+	se->burst_time = 0;
+	update_burst_score(se);
+}
+
+/* BORE toggled off at runtime: clear this entity's burst state + base weight. */
+static void revert_burst(struct sched_entity *se) {
+	se->burst_time = 0;
+	se->prev_burst_penalty = 0;
+	se->curr_burst_penalty = 0;
+	se->burst_penalty = 0;
+	update_burst_score(se);
+}
+#endif // CONFIG_SCHED_BORE
+
 int sched_thermal_decay_shift;
 static int __init setup_sched_thermal_decay_shift(char *str)
 {
@@ -880,7 +972,19 @@ static void update_curr(struct cfs_rq *cfs_rq)
 	curr->sum_exec_runtime += delta_exec;
 	schedstat_add(cfs_rq->exec_clock, delta_exec);
 
+#ifdef CONFIG_SCHED_BORE
+	if (likely(sched_bore)) {
+		curr->burst_time += delta_exec;
+		update_burst_penalty(curr);
+		curr->vruntime += max(1ULL, calc_delta_fair(delta_exec, curr));
+	} else {
+		if (unlikely(curr->burst_penalty))
+			revert_burst(curr);
+		curr->vruntime += calc_delta_fair(delta_exec, curr);
+	}
+#else
 	curr->vruntime += calc_delta_fair(delta_exec, curr);
+#endif // CONFIG_SCHED_BORE
 	update_min_vruntime(cfs_rq);
 
 	if (entity_is_task(curr)) {
@@ -5836,6 +5940,15 @@ static void dequeue_task_fair(struct rq *rq, struct task_struct *p, int flags)
 	int idle_h_nr_running = task_has_idle_policy(p);
 	bool was_sched_idle = sched_idle_rq(rq);
 
+#ifdef CONFIG_SCHED_BORE
+	if (task_sleep && sched_bore) {
+		cfs_rq = cfs_rq_of(se);
+		if (cfs_rq->curr == se)
+			update_curr(cfs_rq);
+		restart_burst(se);
+	}
+#endif // CONFIG_SCHED_BORE
+
 	util_est_dequeue(&rq->cfs, p);
 
 	for_each_sched_entity(se) {
@@ -7540,6 +7653,10 @@ static void yield_task_fair(struct rq *rq)
 		 * Update run-time statistics of the 'current'.
 		 */
 		update_curr(cfs_rq);
+#ifdef CONFIG_SCHED_BORE
+		if (sched_bore)
+			restart_burst(se);
+#endif // CONFIG_SCHED_BORE
 		/*
 		 * Tell update_rq_clock() that we've just updated,
 		 * so we don't do microscopic update in schedule()
@@ -11139,6 +11256,10 @@ static void task_fork_fair(struct task_struct *p)
 		update_curr(cfs_rq);
 		se->vruntime = curr->vruntime;
 	}
+#ifdef CONFIG_SCHED_BORE
+	if (sched_bore)
+		update_burst_score(se);
+#endif // CONFIG_SCHED_BORE
 	place_entity(cfs_rq, se, 1);
 
 	if (sysctl_sched_child_runs_first && curr && entity_before(curr, se)) {
diff --git a/kernel/sysctl.c b/kernel/sysctl.c
index 2a675fb7e..f26b51a8b 100644
--- a/kernel/sysctl.c
+++ b/kernel/sysctl.c
@@ -1743,7 +1743,64 @@ int proc_do_static_key(struct ctl_table *table, int write,
 	return ret;
 }
 
+#ifdef CONFIG_SCHED_BORE
+extern uint sched_bore;
+extern uint sched_burst_smoothness_long;
+extern uint sched_burst_smoothness_short;
+extern uint sched_burst_penalty_offset;
+extern uint sched_burst_penalty_scale;
+static int __maybe_unused sixty_four     = 64;
+static int __maybe_unused maxval_12_bits = 4095;
+#endif // CONFIG_SCHED_BORE
+
 static struct ctl_table kern_table[] = {
+#ifdef CONFIG_SCHED_BORE
+	{
+		.procname	= "sched_bore",
+		.data		= &sched_bore,
+		.maxlen		= sizeof(uint),
+		.mode		= 0644,
+		.proc_handler	= proc_douintvec_minmax,
+		.extra1		= SYSCTL_ZERO,
+		.extra2		= SYSCTL_ONE,
+	},
+	{
+		.procname	= "sched_burst_smoothness_long",
+		.data		= &sched_burst_smoothness_long,
+		.maxlen		= sizeof(uint),
+		.mode		= 0644,
+		.proc_handler	= proc_douintvec_minmax,
+		.extra1		= SYSCTL_ZERO,
+		.extra2		= SYSCTL_ONE,
+	},
+	{
+		.procname	= "sched_burst_smoothness_short",
+		.data		= &sched_burst_smoothness_short,
+		.maxlen		= sizeof(uint),
+		.mode		= 0644,
+		.proc_handler	= proc_douintvec_minmax,
+		.extra1		= SYSCTL_ZERO,
+		.extra2		= SYSCTL_ONE,
+	},
+	{
+		.procname	= "sched_burst_penalty_offset",
+		.data		= &sched_burst_penalty_offset,
+		.maxlen		= sizeof(uint),
+		.mode		= 0644,
+		.proc_handler	= proc_douintvec_minmax,
+		.extra1		= SYSCTL_ZERO,
+		.extra2		= &sixty_four,
+	},
+	{
+		.procname	= "sched_burst_penalty_scale",
+		.data		= &sched_burst_penalty_scale,
+		.maxlen		= sizeof(uint),
+		.mode		= 0644,
+		.proc_handler	= proc_douintvec_minmax,
+		.extra1		= SYSCTL_ZERO,
+		.extra2		= &maxval_12_bits,
+	},
+#endif // CONFIG_SCHED_BORE
 	{
 		.procname	= "sched_child_runs_first",
 		.data		= &sysctl_sched_child_runs_first,
